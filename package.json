{
  "name": "aswe-flyweight",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "build-deploy": "rm -rf dist && vue-tsc --noEmit && vite build && git add dist -f && git commit -m 'adding dist' && git subtree push --prefix dist origin gh-pages"
  },
  "dependencies": {
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.0.0",
    "typescript": "^4.4.4",
    "vite": "^2.7.2",
    "vue-tsc": "^0.29.8"
  }
}